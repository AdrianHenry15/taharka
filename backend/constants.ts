export enum CartStatus {
  PROCESSING = "Processing",
  SHIPPED = "Shipped",
  DELIVERED = "Delivered",
  CANCELLED = "Canceled",
  NOT_PROCESSED = "Not Processed",
}

export enum EmailProvider {
  EMAIL = "Email",
  GOOGLE = "Google",
  FACEBOOK = "Facebook",
}
